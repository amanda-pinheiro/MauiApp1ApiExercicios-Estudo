<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1ApiExercicios.ListaExerciciosPage"
             Title="Meus Exercícios Salvos">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Cabeçalho -->
        <StackLayout Grid.Row="0" Padding="20,10" BackgroundColor="DarkBlue">
            <Label Text="💪 Minha Lista de Exercícios" 
                   FontSize="22" 
                   FontAttributes="Bold"
                   TextColor="White"
                   HorizontalOptions="Center"/>
            <Label x:Name="lbl_contador"
                   Text="0 exercícios salvos" 
                   FontSize="14"
                   TextColor="LightGray"
                   HorizontalOptions="Center"/>
        </StackLayout>

        <!-- Lista de exercícios salvos -->
        <ScrollView Grid.Row="1">
            <StackLayout>
                <!-- Mensagem quando lista está vazia -->
                <StackLayout x:Name="stack_lista_vazia" 
                           IsVisible="True"
                           Padding="40"
                           VerticalOptions="CenterAndExpand">
                    <Label Text="📝" 
                           FontSize="50" 
                           HorizontalOptions="Center"/>
                    <Label Text="Sua lista está vazia!" 
                           FontSize="18" 
                           FontAttributes="Bold"
                           HorizontalOptions="Center"
                           Margin="0,10,0,5"/>
                    <Label Text="Volte para a tela inicial e adicione alguns exercícios." 
                           FontSize="14" 
                           TextColor="Gray"
                           HorizontalOptions="Center"
                           HorizontalTextAlignment="Center"/>
                </StackLayout>

                <!-- ListView com os exercícios -->
                <ListView x:Name="listview_exercicios_salvos"
                          IsVisible="False"
                          Margin="10">
                    <ListView.ItemTemplate>
                        <DataTemplate>
                            <ViewCell>
                                <Frame Padding="15" 
                                       Margin="5" 
                                       BackgroundColor="White"
                                       BorderColor="LightGray"
                                       CornerRadius="10">
                                    <Grid>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                            <RowDefinition Height="Auto"/>
                                        </Grid.RowDefinitions>
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="60"/>
                                            <ColumnDefinition Width="*"/>
                                            <ColumnDefinition Width="Auto"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Imagem do exercício -->
                                        <Frame Grid.Row="0" Grid.Column="0" Grid.RowSpan="4"
                                               WidthRequest="60" 
                                               HeightRequest="60"
                                               BackgroundColor="LightBlue"
                                               CornerRadius="30"
                                               Padding="2"
                                               VerticalOptions="Start">
                                            <Image Source="{Binding GifUrl}"
                                                   Aspect="AspectFill"/>
                                        </Frame>

                                        <!-- Nome do exercício -->
                                        <Label Grid.Row="0" Grid.Column="1" 
                                               Text="{Binding Name}" 
                                               FontSize="18" 
                                               FontAttributes="Bold"
                                               TextColor="DarkBlue"/>

                                        <!-- Alvo do exercício -->
                                        <Label Grid.Row="1" Grid.Column="1" 
                                               Text="{Binding Target, StringFormat='🎯 Alvo: {0}'}" 
                                               FontSize="14" 
                                               TextColor="Gray"
                                               Margin="0,2,0,0"/>

                                        <!-- Parte do corpo -->
                                        <Label Grid.Row="2" Grid.Column="1" 
                                               Text="{Binding BodyPart, StringFormat='💪 Parte: {0}'}" 
                                               FontSize="14" 
                                               TextColor="Gray"
                                               Margin="0,2,0,0"/>

                                        <!-- Equipamento -->
                                        <Label Grid.Row="3" Grid.Column="1" 
                                               Text="{Binding Equipment, StringFormat='🔧 Equipamento: {0}'}" 
                                               FontSize="14" 
                                               TextColor="Gray"
                                               Margin="0,2,0,0"/>

                                        <!-- Botão de remover -->
                                        <Button Grid.Row="0" Grid.Column="2" Grid.RowSpan="4"
                                                Text="🗑️"
                                                FontSize="16"
                                                BackgroundColor="LightCoral"
                                                TextColor="White"
                                                WidthRequest="40"
                                                HeightRequest="40"
                                                CornerRadius="20"
                                                Clicked="OnRemoveExerciseClicked"/>
                                    </Grid>
                                </Frame>
                            </ViewCell>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                </ListView>
            </StackLayout>
        </ScrollView>

        <!-- Rodapé com botões -->
        <StackLayout Grid.Row="2" 
                     Orientation="Horizontal" 
                     Padding="10" 
                     BackgroundColor="LightGray"
                     Spacing="10">

            <Button Text="⬅️ Voltar" 
                    BackgroundColor="Gray"
                    TextColor="White"
                    HorizontalOptions="FillAndExpand"
                    Clicked="OnVoltarClicked"/>

            <Button Text="🗑️ Limpar Tudo" 
                    BackgroundColor="Red"
                    TextColor="White"
                    HorizontalOptions="FillAndExpand"
                    Clicked="OnLimparTudoClicked"/>
        </StackLayout>
    </Grid>
</ContentPage>